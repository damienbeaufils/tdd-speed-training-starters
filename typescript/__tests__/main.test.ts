describe('main', () => {
  describe('hello()', () => {
    it('should do or return this when that', () => {
      // given
      const expected = true;

      // when
      const result = false;

      // then
      expect(result).toBe(expected);
    });
  });
});
